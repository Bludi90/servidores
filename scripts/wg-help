#!/usr/bin/env bash
set -e
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
if [ $# -eq 0 ]; then
  "$ROOT/scripts/srv-help" wireguard
else
  [ "$1" = "-h" ] || [ "$1" = "--help" ] && { echo "Uso: wg-help [subcomando]"; exit 0; }
  "$ROOT/scripts/srv-help" wireguard "$1"
fi
